import { RestrictDashDirective } from './restrict-dash.directive';

describe('RestrictDashDirective', () => {
  it('should create an instance', () => {
    const directive = new RestrictDashDirective();
    expect(directive).toBeTruthy();
  });
});
